# Setup Instructions (Ishan Shirode – HR RL System)

1. Create a virtual environment:
   python -m venv venv
   venv\Scripts\activate

2. Install dependencies:
   pip install -r requirements.txt

3. Run the application:
   python app.py

4. Logs and Learning Data:
   - Feedback logs: /feedback/feedback_log.csv
   - Models: /models/sentiment_model.pkl, tfidf_model.pkl

5. Output:
   The console prints RL learning updates and stores new feedback scores.
